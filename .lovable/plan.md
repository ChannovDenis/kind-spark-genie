
# План реализации: Список диалогов для аудита `/quality/dialogs`

## Цель

Создать страницу очереди диалогов для QA-аудита — централизованный интерфейс для команды качества, где можно находить, фильтровать и отправлять диалоги на детальный разбор. Это критически важная страница, соединяющая дашборд Quality Center с детальным разбором `/quality/dialogs/:id`.

---

## Исследование лучших практик

### Изучены инструменты
| Инструмент | Ключевые UX-паттерны |
|------------|---------------------|
| **Zendesk QA** | Кастомные фильтры, сохраняемые пресеты, риск-скоринг, spotlight conversations |
| **Label Studio** | Очереди с прогрессом, assignment workflow, keyboard-first navigation |
| **Arize AX** | Labeling queues с criteria-based assignment, annotator progress tracking |
| **Moderation API** | Configurable queues, webhooks, bulk actions |
| **Jira Service Management** | SLA-индикаторы, приоритизация, сортировка внутри очередей |

### Ключевые инсайты для внедрения

1. **Smart Filters / Saved Views** — пользователи создают и сохраняют свои пресеты фильтров ("Критичные за сегодня", "Низкий AI Score врачей")

2. **Risk-based Prioritization** — автоматическое ранжирование по риску: AI Score + категория ошибки + сервис

3. **Assignment Workflow** — назначение диалогов на конкретных ревьюеров, прогресс каждого

4. **Keyboard Navigation** — `↑`/`↓` навигация, `Enter` открыть, `S` отправить на дообучение

5. **Bulk Selection** — чекбоксы для массовых действий: "Пометить обработанными", "В дообучение"

6. **Visual Status Indicators** — цветовые индикаторы: зелёный (OK), жёлтый (внимание), красный (критично)

7. **Time-based Filters** — "Сегодня", "Вчера", "Эта неделя", "Кастомный период"

8. **Stats Panel** — мини-статистика прямо на странице: сколько обработано, среднее время на диалог

---

## Архитектура страницы

### Layout
```text
+-----------------------------------------------------------------------------+
|  PageHeader: "Диалоги на разбор"  +  [Сохранённые фильтры ▼]  [Назначить]   |
+-----------------------------------------------------------------------------+
|                                                                             |
|  [🔍 Поиск по ID или пользователю...]  [Фильтры (6)]  [Период ▼]  [Сброс]  |
|                                                                             |
+------------------+----------------------------------------------------------+
|                  |                                                          |
|  БЫСТРЫЕ         |     ТАБЛИЦА ДИАЛОГОВ                                    |
|  ФИЛЬТРЫ         |                                                          |
|                  |  ☐  ID      Пользователь   Сервис   Score  Статус  Дата |
|  ───────────     |  ─────────────────────────────────────────────────────── |
|  📊 По статусу   |  ☐  #4521   User #4521     Юрист    92%    ✓ OK    10:30 |
|  • Все (1,234)   |  ☐  #4520   User #4520     Врач     45%    ⚠ Review 09:15|
|  • На проверке   |  ☐  #4519   User #4519     Психолог 78%    ⚠ Review 08:45|
|    (156)         |  ☐  #4518   User #4518     Финанс.  95%    ✓ OK    Вчера |
|  • Обработано    |  ☐  #4517   User #4517     Юрист    32%    ✗ Critical .. |
|    (1,078)       |                                                          |
|                  |  ─────────────────────────────────────────────────────── |
|  📉 По AI Score  |                                                          |
|  • < 50% (23)    |  [ < Пред. ]  Страница 1 из 124  [ След. > ]            |
|  • 50-80% (156)  |                                                          |
|  • > 80% (1,055) |  ─────────────────────────────────────────────────────── |
|                  |                                                          |
|  🏷️ По сервису   |  Bulk Actions (при выборе):                              |
|  • Юрист (420)   |  [☐ 3 выбрано]  [Обработано]  [В дообучение]  [Назначить]|
|  • Врач (380)    |                                                          |
|  • Психолог (234)|                                                          |
|  • Финансист     |                                                          |
|    (200)         |                                                          |
|                  |                                                          |
|  ───────────     |                                                          |
|  💾 Сохранённые  |                                                          |
|  • Критичные     |                                                          |
|  • Мои сегодня   |                                                          |
|  • [+ Сохранить] |                                                          |
|                  |                                                          |
+------------------+----------------------------------------------------------+
```

---

## Типы данных

### Диалог для аудита
```typescript
export interface QualityDialog {
  id: string;
  
  // Участники
  userId: string;
  userName: string;
  userAvatar?: string;
  
  // Контекст
  service: string;                    // lawyer, doctor, etc.
  tenantId: string;
  
  // Метрики качества
  aiScore: number;                    // 0-100
  messagesCount: number;
  duration: string;                   // "8:45"
  tokensUsed: number;
  avgResponseTime: string;            // "1.2s"
  
  // Проблемы
  flaggedMessagesCount: number;       // сколько сообщений помечено как проблемные
  issues: DialogIssue[];              // список проблем
  
  // Workflow
  status: 'pending' | 'in_review' | 'reviewed' | 'escalated' | 'training';
  assignedTo?: string;                // ID ревьюера
  assignedToName?: string;
  reviewedAt?: string;
  reviewedBy?: string;
  
  // Метаданные
  createdAt: string;
  endedAt: string;
  escalatedToExpert: boolean;
  
  // Для ранжирования
  riskScore: number;                  // 0-100, вычисляется из aiScore + issues
  priority: 'critical' | 'high' | 'medium' | 'low';
}

export interface DialogIssue {
  id: string;
  messageId: string;
  category: 'hallucination' | 'inaccuracy' | 'empathy' | 'protocol' | 'safety';
  description: string;
  severity: 'critical' | 'warning' | 'info';
}

export interface SavedFilter {
  id: string;
  name: string;
  filters: DialogFilters;
  createdBy: string;
  isDefault?: boolean;
}

export interface DialogFilters {
  status: string[];
  services: string[];
  scoreRange: [number, number];       // [0, 100]
  dateRange: [string, string] | null;
  assignedTo: string | null;
  hasIssues: boolean | null;
  issueCategories: string[];
  priority: string[];
  search: string;
}

export interface DialogStats {
  total: number;
  pending: number;
  inReview: number;
  reviewed: number;
  escalated: number;
  
  avgAiScore: number;
  avgReviewTime: string;              // "5m 23s"
  
  byService: Record<string, number>;
  byScoreRange: {
    critical: number;   // < 50
    warning: number;    // 50-80
    good: number;       // > 80
  };
}
```

---

## Компоненты

### 1. Главная страница `DialogList.tsx`
**Структура:**
- PageHeader с действиями (сохранённые фильтры, назначение)
- Строка поиска и фильтров
- Двухколоночный layout: sidebar с быстрыми фильтрами + таблица

### 2. Боковая панель быстрых фильтров `DialogQuickFilters.tsx`
**Функционал:**
- Группы фильтров: По статусу, По AI Score, По сервису
- Счётчики в каждом пункте
- Клик по пункту — фильтрует таблицу
- Визуальная индикация активного фильтра
- Раздел "Сохранённые фильтры" с возможностью создать новый

### 3. Строка поиска и фильтров `DialogSearchBar.tsx`
**Элементы:**
- Input с поиском по ID диалога или имени пользователя
- Кнопка "Фильтры" с badge-счётчиком активных
- DateRangePicker для выбора периода
- Кнопка "Сбросить" (показывается если есть активные фильтры)

### 4. Панель расширенных фильтров `DialogFiltersPanel.tsx`
**Открывается по клику на "Фильтры". Содержит:**
- Multi-select сервисов
- Multi-select статусов
- Slider для диапазона AI Score
- Multi-select категорий ошибок
- Checkbox "Только с проблемами"
- Select "Назначено на"
- Кнопки: Применить, Сбросить, Сохранить как пресет

### 5. Таблица диалогов `DialogTable.tsx`
**Колонки:**
- Checkbox (для bulk select)
- ID диалога (ссылка на детальный разбор)
- Пользователь (аватар + имя)
- Сервис (иконка + название)
- AI Score (цветовой индикатор + процент)
- Проблемы (количество + иконки категорий)
- Статус (badge)
- Дата/время
- Назначено (аватар ревьюера или "—")
- Actions (три точки: Открыть, Назначить, В дообучение)

**Функционал:**
- Сортировка по всем колонкам (клик на заголовок)
- Hover-эффект на строках
- Клик на строку — переход на `/quality/dialogs/:id`
- Sticky header при прокрутке

### 6. Bulk Actions Bar `DialogBulkActions.tsx`
**Появляется при выборе одного или более диалогов:**
- Счётчик выбранных
- Кнопка "Пометить обработанными"
- Кнопка "Отправить в дообучение"
- Кнопка "Назначить на..."
- Кнопка "Отменить выбор"

### 7. Пагинация `DialogPagination.tsx`
- Стандартная пагинация: первая, предыдущая, номера страниц, следующая, последняя
- Select количества на странице: 25, 50, 100

### 8. Модалка назначения `AssignReviewerDialog.tsx`
- Выбор ревьюера из списка команды
- Комментарий для ревьюера (опционально)
- Кнопки: Назначить, Отмена

### 9. Модалка сохранения фильтра `SaveFilterDialog.tsx`
- Input для названия фильтра
- Checkbox "Сделать по умолчанию"
- Кнопки: Сохранить, Отмена

---

## Mock-данные

```typescript
// dialogsData.ts
export const qualityDialogs: QualityDialog[] = [
  {
    id: 'dlg-4521',
    userId: 'u-1001',
    userName: 'Сергей Иванов',
    service: 'lawyer',
    tenantId: 'gazprombank',
    aiScore: 92,
    messagesCount: 8,
    duration: '6:30',
    tokensUsed: 1850,
    avgResponseTime: '0.9s',
    flaggedMessagesCount: 0,
    issues: [],
    status: 'reviewed',
    reviewedAt: '2026-02-08T11:00:00',
    reviewedBy: 'Анна Козлова',
    createdAt: '2026-02-08T10:30:00',
    endedAt: '2026-02-08T10:36:30',
    escalatedToExpert: false,
    riskScore: 8,
    priority: 'low',
  },
  {
    id: 'dlg-4520',
    userId: 'u-1002',
    userName: 'Мария Петрова',
    service: 'doctor',
    tenantId: 'gazprombank',
    aiScore: 45,
    messagesCount: 6,
    duration: '8:45',
    tokensUsed: 2340,
    avgResponseTime: '1.2s',
    flaggedMessagesCount: 2,
    issues: [
      { 
        id: 'iss-1', 
        messageId: 'msg-4', 
        category: 'inaccuracy', 
        description: 'Преждевременный диагноз',
        severity: 'critical'
      },
      { 
        id: 'iss-2', 
        messageId: 'msg-4', 
        category: 'protocol', 
        description: 'Не уточнён анамнез',
        severity: 'warning'
      }
    ],
    status: 'pending',
    createdAt: '2026-02-08T09:15:00',
    endedAt: '2026-02-08T09:23:45',
    escalatedToExpert: false,
    riskScore: 78,
    priority: 'critical',
  },
  // ... 50+ диалогов
];

export const savedFilters: SavedFilter[] = [
  {
    id: 'sf-1',
    name: 'Критичные сегодня',
    filters: {
      status: ['pending'],
      services: [],
      scoreRange: [0, 50],
      dateRange: ['today', 'today'],
      assignedTo: null,
      hasIssues: true,
      issueCategories: [],
      priority: ['critical', 'high'],
      search: '',
    },
    createdBy: 'system',
    isDefault: false,
  },
  {
    id: 'sf-2',
    name: 'Мои на проверке',
    filters: {
      status: ['in_review'],
      services: [],
      scoreRange: [0, 100],
      dateRange: null,
      assignedTo: 'current_user',
      hasIssues: null,
      issueCategories: [],
      priority: [],
      search: '',
    },
    createdBy: 'user',
    isDefault: true,
  },
  {
    id: 'sf-3',
    name: 'Психолог: эмпатия',
    filters: {
      status: [],
      services: ['psychologist'],
      scoreRange: [0, 100],
      dateRange: null,
      assignedTo: null,
      hasIssues: true,
      issueCategories: ['empathy'],
      priority: [],
      search: '',
    },
    createdBy: 'user',
  },
];

export const dialogStats: DialogStats = {
  total: 1234,
  pending: 156,
  inReview: 45,
  reviewed: 989,
  escalated: 44,
  
  avgAiScore: 78.5,
  avgReviewTime: '5m 23s',
  
  byService: {
    lawyer: 420,
    doctor: 380,
    psychologist: 234,
    financier: 200,
  },
  byScoreRange: {
    critical: 23,
    warning: 156,
    good: 1055,
  },
};

export const reviewers = [
  { id: 'rev-1', name: 'Анна Козлова', avatar: '', activeDialogs: 12 },
  { id: 'rev-2', name: 'Мария Иванова', avatar: '', activeDialogs: 8 },
  { id: 'rev-3', name: 'Алексей Петров', avatar: '', activeDialogs: 15 },
  { id: 'rev-4', name: 'Елена Смирнова', avatar: '', activeDialogs: 5 },
];
```

---

## Горячие клавиши

| Клавиша | Действие |
|---------|----------|
| `↑` / `↓` | Навигация по списку (фокус на строке) |
| `Enter` | Открыть выбранный диалог |
| `Space` | Toggle checkbox на выбранной строке |
| `Ctrl+A` | Выбрать все на странице |
| `Escape` | Сбросить выделение |
| `S` | Отправить выбранные в дообучение |
| `D` | Пометить выбранные как обработанные |
| `/` | Фокус на поиск |
| `F` | Открыть панель фильтров |

---

## Новые файлы

| Файл | Описание |
|------|----------|
| `src/pages/quality/DialogList.tsx` | Главная страница списка диалогов |
| `src/components/quality/DialogQuickFilters.tsx` | Боковая панель быстрых фильтров |
| `src/components/quality/DialogSearchBar.tsx` | Строка поиска и кнопок фильтрации |
| `src/components/quality/DialogFiltersPanel.tsx` | Расширенная панель фильтров (Sheet) |
| `src/components/quality/DialogTable.tsx` | Таблица диалогов |
| `src/components/quality/DialogTableRow.tsx` | Строка таблицы с данными диалога |
| `src/components/quality/DialogBulkActions.tsx` | Панель массовых действий |
| `src/components/quality/AssignReviewerDialog.tsx` | Модалка назначения ревьюера |
| `src/components/quality/SaveFilterDialog.tsx` | Модалка сохранения фильтра |
| `src/data/dialogsData.ts` | Типы и mock-данные диалогов |

---

## Модификации существующих файлов

| Файл | Изменения |
|------|-----------|
| `src/App.tsx` | Добавить маршрут `/quality/dialogs` -> `DialogList` |
| `src/pages/PlaceholderPages.tsx` | Удалить `QualityDialogs` placeholder |
| `src/pages/quality/Dashboard.tsx` | Обновить ссылки на `/quality/dialogs/:id` |
| `src/hooks/useKeyboardShortcuts.ts` | Возможно потребуется расширить для новых шорткатов |

---

## Используемые UI-компоненты

| Компонент | Назначение |
|-----------|------------|
| `Table` | Таблица диалогов |
| `Checkbox` | Выбор для bulk actions |
| `Input` | Поиск |
| `Select` | Фильтры |
| `Sheet` | Панель расширенных фильтров |
| `Dialog` | Модалки назначения и сохранения |
| `Badge` | Статусы, счётчики |
| `Button` | Действия |
| `Slider` | Диапазон AI Score |
| `Popover` | Выпадающие меню |
| `DropdownMenu` | Actions на строке |
| `Avatar` | Пользователи, ревьюеры |
| `ScrollArea` | Прокрутка боковой панели |
| `Separator` | Разделители в sidebar |

---

## Стилистика

- Layout: `grid grid-cols-[260px_1fr]`
- Sidebar: `bg-sidebar border-r`
- Таблица: стандартные стили `data-table` из проекта
- AI Score индикатор: 
  - < 50%: `text-destructive` + красный dot
  - 50-80%: `text-warning` + жёлтый dot
  - > 80%: `text-success` + зелёный dot
- Статусы:
  - pending: `bg-warning/10 text-warning`
  - in_review: `bg-blue-500/10 text-blue-500`
  - reviewed: `bg-success/10 text-success`
  - escalated: `bg-purple-500/10 text-purple-500`
  - training: `bg-primary/10 text-primary`

---

## Взаимодействия

1. **Поиск** — debounced input, фильтрует по ID или userName
2. **Быстрые фильтры** — клик переключает фильтр, множественный выбор
3. **Расширенные фильтры** — открывает Sheet справа
4. **Клик по строке** — переход на `/quality/dialogs/:id`
5. **Checkbox** — добавляет в выбранные для bulk actions
6. **Сортировка** — клик на заголовок колонки
7. **Пагинация** — стандартная навигация
8. **Сохранение фильтра** — открывает диалог, сохраняет в sidebar
9. **Назначение** — открывает диалог выбора ревьюера

---

## Порядок реализации

1. Создать `src/data/dialogsData.ts` — типы и mock-данные
2. Создать `DialogTable.tsx` — основная таблица
3. Создать `DialogTableRow.tsx` — строка с данными
4. Создать `DialogQuickFilters.tsx` — боковая панель
5. Создать `DialogSearchBar.tsx` — поиск и кнопки
6. Создать `DialogFiltersPanel.tsx` — расширенные фильтры
7. Создать `DialogBulkActions.tsx` — панель массовых действий
8. Создать `AssignReviewerDialog.tsx` — модалка назначения
9. Собрать `DialogList.tsx` — главная страница
10. Обновить маршруты в `App.tsx`
11. Удалить placeholder

---

## Результат

После реализации команда качества получит:

1. **Централизованную очередь** всех диалогов для аудита
2. **Гибкую фильтрацию** по 8+ параметрам с сохранением пресетов
3. **Быстрый поиск** по ID и пользователю
4. **Визуальную приоритизацию** — сразу видно критичные диалоги
5. **Массовые действия** для ускорения работы
6. **Keyboard-first UX** для power users
7. **Workflow назначения** — распределение нагрузки между ревьюерами
8. **Прозрачную статистику** — сколько обработано, среднее время
