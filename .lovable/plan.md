

# План реализации: База знаний Quality Center

## Цель

Создать страницу `/quality/knowledge` — централизованное хранилище знаний для AI: документы, FAQ, протоколы по сервисам. Команда качества и AI-тренеры смогут управлять контентом, который использует AI при ответах.

---

## Архитектура страницы

### Layout: три колонки

```text
+-----------------------------------------------------------------------+
|  PageHeader: "База знаний"  +  [+ Добавить документ]  [Импорт]        |
+-----------------------------------------------------------------------+
|                                                                       |
|  [🔍 Поиск по документам и FAQ...                              ]      |
|                                                                       |
+------------------+--------------------------------+--------------------+
|                  |                                |                    |
|  ДЕРЕВО          |     КОНТЕНТ                   |    СВОЙСТВА        |
|  КАТЕГОРИЙ       |                               |                    |
|                  |                               |                    |
|  ▼ 📁 Юрист      |   Заголовок документа         |  Статус: Активен   |
|    📄 Права      |   ─────────────────────       |  Сервис: Юрист     |
|    📄 Договоры   |                               |  Тип: Протокол     |
|    📄 Налоги     |   Содержимое документа        |  Создан: 5 фев     |
|  ▼ 📁 Врач       |   с markdown-разметкой...     |  Изменён: 8 фев    |
|    📄 Симптомы   |                               |                    |
|    📄 Протоколы  |   ## Раздел                   |  ───────────────   |
|  ▶ 📁 Психолог   |   - Пункт 1                   |                    |
|  ▶ 📁 Финансист  |   - Пункт 2                   |  Используется в:   |
|                  |                               |  • 1,234 диалогах  |
|  ───────────     |                               |  • 89% релевантно  |
|                  |                               |                    |
|  ▼ 📋 FAQ        |   [Редактировать] [Удалить]   |  [В архив]         |
|    ❓ Общие      |                               |  [Дублировать]     |
|    ❓ По юристу  |                               |                    |
|                  |                               |                    |
|  ▼ 📜 Протоколы  +--------------------------------+                    |
|    ⚡ Эскалация  |                               |                    |
|    ⚡ Экстренные |   [Табы: Документ | FAQ | История]                  |
|                  |                               |                    |
+------------------+-------------------------------+--------------------+
```

---

## Компоненты

### 1. Дерево категорий `CategoryTree`

**Структура категорий:**
- **Сервисы** (папки по каждому сервису из `mockData.services`)
  - Документы внутри каждого сервиса
- **FAQ** — часто задаваемые вопросы
  - Общие FAQ
  - FAQ по сервисам
- **Протоколы** — инструкции для AI
  - Протокол эскалации
  - Экстренные ситуации
  - Границы компетенции

**Функционал:**
- Сворачивание/разворачивание папок (Collapsible)
- Drag & drop для перемещения документов (опционально, mock)
- Контекстное меню: переименовать, удалить, создать подкатегорию
- Счётчик документов в каждой категории
- Иконки по типу: 📁 папка, 📄 документ, ❓ FAQ, ⚡ протокол

### 2. Поиск `KnowledgeSearch`

- Input с иконкой поиска
- Поиск по заголовкам и содержимому
- Фильтры: тип (документ/FAQ/протокол), сервис, статус
- Результаты с подсветкой найденного текста
- Быстрые теги: "Недавние", "Популярные", "Требуют обновления"

### 3. Просмотрщик контента `DocumentViewer`

**Табы:**
- **Документ** — markdown-контент с подсветкой синтаксиса
- **FAQ** — список Q&A с возможностью редактирования inline
- **История** — лог изменений документа

**Функционал:**
- Markdown-рендеринг (заголовки, списки, таблицы, код)
- Кнопки: Редактировать, Удалить, В архив
- Предпросмотр как видит AI
- Копирование в буфер

### 4. Редактор документа `DocumentEditor` (диалог)

**Поля:**
- Заголовок
- Категория (Select из дерева)
- Сервис (мульти-выбор)
- Тип: Документ / FAQ / Протокол
- Содержимое (Textarea с markdown-превью)
- Теги (мульти-инпут)
- Приоритет: Высокий / Средний / Низкий

**Для FAQ:**
- Вопрос
- Ответ
- Альтернативные формулировки вопроса

### 5. Панель свойств `DocumentProperties`

- Метаданные: создан, изменён, автор
- Статус: Активен / Черновик / Архив
- Статистика использования:
  - В скольких диалогах использован
  - Средняя релевантность
  - Последнее использование
- Связанные документы
- Быстрые действия

---

## Типы данных

```typescript
// Категория в дереве
interface KnowledgeCategory {
  id: string;
  name: string;
  icon: string;
  type: 'folder' | 'service' | 'faq' | 'protocol';
  parentId: string | null;
  children?: KnowledgeCategory[];
  documentCount: number;
}

// Документ базы знаний
interface KnowledgeDocument {
  id: string;
  title: string;
  content: string;          // markdown
  type: 'document' | 'faq' | 'protocol';
  categoryId: string;
  services: string[];       // ID сервисов
  status: 'active' | 'draft' | 'archived';
  priority: 'high' | 'medium' | 'low';
  tags: string[];
  createdAt: string;
  updatedAt: string;
  author: string;
  usageStats: {
    dialogsCount: number;
    relevanceScore: number;
    lastUsed: string;
  };
}

// FAQ-элемент
interface FAQItem {
  id: string;
  question: string;
  answer: string;
  alternativeQuestions: string[];
  categoryId: string;
  services: string[];
  usageCount: number;
}

// Протокол
interface Protocol {
  id: string;
  name: string;
  description: string;
  rules: ProtocolRule[];
  triggers: string[];       // ключевые слова-триггеры
  priority: number;         // порядок применения
}

interface ProtocolRule {
  id: string;
  condition: string;
  action: 'escalate' | 'warn' | 'block' | 'suggest';
  message: string;
}
```

---

## Mock-данные

### Категории
```typescript
const knowledgeCategories = [
  {
    id: 'cat-lawyer',
    name: 'Юрист',
    icon: '⚖️',
    type: 'service',
    parentId: null,
    documentCount: 24,
    children: [
      { id: 'cat-lawyer-rights', name: 'Права потребителя', documentCount: 8 },
      { id: 'cat-lawyer-contracts', name: 'Договоры', documentCount: 12 },
      { id: 'cat-lawyer-taxes', name: 'Налоги', documentCount: 4 },
    ]
  },
  // ... другие сервисы
  {
    id: 'cat-faq',
    name: 'FAQ',
    icon: '❓',
    type: 'faq',
    documentCount: 156,
  },
  {
    id: 'cat-protocols',
    name: 'Протоколы',
    icon: '📜',
    type: 'protocol',
    documentCount: 12,
  },
];
```

### Документы
```typescript
const knowledgeDocuments = [
  {
    id: 'doc-1',
    title: 'Возврат товара надлежащего качества',
    content: `# Возврат товара надлежащего качества

## Основные положения

Согласно статье 25 Закона о защите прав потребителей...

### Сроки возврата
- 14 дней с момента покупки
- Товар не был в употреблении
- Сохранены товарный вид и пломбы

### Исключения
Не подлежат возврату:
1. Товары личной гигиены
2. Лекарственные препараты
3. ...`,
    type: 'document',
    categoryId: 'cat-lawyer-rights',
    services: ['lawyer'],
    status: 'active',
    priority: 'high',
    tags: ['возврат', 'права потребителя', 'закон'],
    usageStats: {
      dialogsCount: 1234,
      relevanceScore: 0.89,
      lastUsed: '2026-02-08T10:30:00',
    }
  },
  // ... другие документы
];
```

### Протоколы
```typescript
const protocols = [
  {
    id: 'proto-escalation',
    name: 'Протокол эскалации',
    description: 'Когда передавать клиента живому эксперту',
    rules: [
      {
        id: 'rule-1',
        condition: 'Клиент явно просит живого специалиста',
        action: 'escalate',
        message: 'Понимаю, сейчас соединю вас с экспертом.',
      },
      {
        id: 'rule-2',
        condition: 'Вопрос требует изучения документов клиента',
        action: 'escalate',
        message: 'Для анализа ваших документов потребуется консультация эксперта.',
      },
    ],
    triggers: ['хочу специалиста', 'живой человек', 'не понимаю'],
    priority: 1,
  },
  {
    id: 'proto-emergency',
    name: 'Экстренные ситуации',
    description: 'Реакция на кризисные ситуации',
    rules: [
      {
        id: 'rule-em-1',
        condition: 'Упоминание суицида или самоповреждения',
        action: 'escalate',
        message: 'Я слышу, что вам сейчас очень тяжело. Давайте я соединю вас со специалистом.',
      },
    ],
    triggers: ['не хочу жить', 'покончить', 'суицид'],
    priority: 0, // максимальный приоритет
  },
];
```

---

## Новые файлы

| Файл | Описание |
|------|----------|
| `src/pages/quality/Knowledge.tsx` | Главная страница базы знаний |
| `src/components/quality/CategoryTree.tsx` | Дерево категорий с Collapsible |
| `src/components/quality/KnowledgeSearch.tsx` | Компонент поиска с фильтрами |
| `src/components/quality/DocumentViewer.tsx` | Просмотрщик с табами |
| `src/components/quality/DocumentEditor.tsx` | Диалог редактирования |
| `src/components/quality/DocumentProperties.tsx` | Панель свойств справа |
| `src/components/quality/FAQEditor.tsx` | Редактор FAQ-элементов |
| `src/components/quality/ProtocolViewer.tsx` | Просмотрщик протоколов |
| `src/data/knowledgeData.ts` | Mock-данные базы знаний |

---

## Модификации существующих файлов

| Файл | Изменения |
|------|-----------|
| `src/App.tsx` | Заменить placeholder на новый компонент |
| `src/pages/PlaceholderPages.tsx` | Удалить `QualityKnowledge` |

---

## Используемые компоненты

| Компонент | Назначение |
|-----------|------------|
| `Collapsible` | Сворачиваемые категории в дереве |
| `ScrollArea` | Прокрутка дерева и контента |
| `Tabs` | Переключение Документ/FAQ/История |
| `Dialog` | Редактор документа |
| `Input` | Поиск, заголовок |
| `Textarea` | Контент документа |
| `Select` | Выбор категории, сервиса, типа |
| `Badge` | Статус, теги |
| `Button` | Действия |
| `Separator` | Разделители |
| `ContextMenu` | Правый клик на категории |
| `Progress` | Релевантность |

---

## Стилистика

- Три колонки: `grid grid-cols-[280px_1fr_300px]`
- Левая колонка: фон `bg-sidebar`, бордер справа
- Правая колонка: фон `bg-muted/30`, бордер слева
- Дерево: отступы для вложенности `pl-4` на уровень
- Активный документ: `bg-primary/10 border-l-2 border-primary`
- Иконки категорий: эмодзи + цветной акцент по сервису

---

## Взаимодействия

1. **Клик по категории** — разворачивает/сворачивает
2. **Клик по документу** — загружает в центральную панель
3. **Двойной клик** — открывает редактор
4. **Поиск** — фильтрует дерево + показывает результаты
5. **Кнопка "Добавить"** — открывает редактор с выбором типа
6. **Drag & drop** — перемещение между категориями (визуальный mock)

---

## Порядок реализации

1. Создать `src/data/knowledgeData.ts` с типами и mock-данными
2. Создать `CategoryTree` — дерево категорий
3. Создать `DocumentViewer` — просмотрщик контента
4. Создать `DocumentProperties` — панель свойств
5. Создать `DocumentEditor` — диалог редактирования
6. Создать `KnowledgeSearch` — поиск с фильтрами
7. Собрать `Knowledge.tsx` — главная страница
8. Обновить маршруты в `App.tsx`
9. Удалить placeholder из `PlaceholderPages.tsx`

---

## Результат

После реализации команда качества сможет:
1. Просматривать все документы базы знаний в структурированном дереве
2. Искать по содержимому и фильтровать по типу/сервису
3. Редактировать документы с markdown-разметкой
4. Управлять FAQ с альтернативными формулировками
5. Настраивать протоколы эскалации и экстренных ситуаций
6. Видеть статистику использования каждого документа AI
7. Архивировать устаревшие материалы

